package s0020;

/**
 * @author KK
 * @create 2021/11/16 20:24
 */
public class IsValid {
    /**
     * 给定一个只包含字符'('，')'，'{'，'}'，'['和']'的字符串，判断输入字符串是否有效。
     *
     * 输入字符串在以下情况下有效:
     *
     * 开括号必须用相同类型的括号关闭。 开括号必须按正确的顺序关闭。 注意，空字符串也被认为是有效的。
     *
     * @param s 给定的字符串
     * @return true OR false (默认无效)
     */
    public static boolean isValid(String s){
        return false;
    }
}
